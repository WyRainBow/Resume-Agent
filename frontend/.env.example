# Resume-Agent frontend environment template
# only local + remote-dev

VITE_ENV_DEFAULT=local
VITE_API_BASE_URL_LOCAL=http://localhost:9000
VITE_API_BASE_URL_REMOTE_DEV=https://resumegenkk.xyz
VITE_AGENT_HISTORY_APPEND_MODE=true
# 本地联调默认通过 Vite 代理到 9000
# 若看到 /api/logos 或 /api/agent/history/* 500，请先检查后端与迁移状态。
