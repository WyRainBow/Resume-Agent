# 线上数据库同步 Dry Run 记录

日期：2026-05-22

## 目标

将本地开发数据库中的业务数据同步到线上数据库，同时不修改线上数据库表结构。

## 安全约束

- 不输出线上数据库连接串、账号或密码。
- 不执行 `create_all`、Alembic migration、DDL 或删表操作。
- 先执行只读 dry-run，确认本地与线上数据差异后再决定是否写入。

## 检查结果

- 本地 SQLite 必要表存在：`users`、`resumes`。
- 线上数据库必要表存在：`users`、`resumes`。
- 本地 `users`：0 条。
- 本地 `resumes`：0 条。
- 线上 `users`：51 条。
- 线上 `resumes`：77 条。

## 执行结论

本地没有可同步的业务数据，因此没有执行任何线上写入。

本次操作未修改线上数据库表结构，未插入、更新或删除线上数据。
