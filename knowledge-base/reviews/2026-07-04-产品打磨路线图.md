# 产品打磨路线图：方法论 + 已完成 + 未做建议清单

> 日期：2026-07-04
> 用途：产品优化的**单一待办与方法论文档**。做完一项就把它从「未做」挪到「已完成」。
> 关联：[[2026-07-03-刘小排产品思维与Resume-Agent产品设计]]（方法论全文）· [[2026-07-03-优化经历功能-bug排查与设计建议]]（优化功能专项）

---

## 一、方法论（做任何功能前对照）

### 刘小排核心思想（浓缩版）

| 原则 | 一句话 |
|---|---|
| 真需求 = 愿意付出代价 | 越靠近钱、时间、风险、职业机会，付费意愿越强 |
| 订阅是问题的时间结构 | 一年一次的问题撑不起订阅——简历天然低频，要么单次收费，要么把低频做成高频 |
| 核心动作明确 | 用户一句话能理解产品；一屏回答一个问题、一个主 CTA |
| 买时间 | 首屏只争取「继续看下去」；每一步只争取下一步 |
| 结果直接可见 | 做完立刻看到效果（右侧预览 / 对比卡 / before-after） |
| 价值先行、付费后置 | 付费引导放在「已感受到价值 + 刚好需要更高权益」的时刻，不突然弹窗 |
| 黄金规则 | 用户明确说要做的事直接动手，不反复确认；范围模糊才问一次 |
| 打磨优先级 | ①商业闭环（首屏→登录→额度→支付→权益）②增长（SEO/案例）③高级功能（团队/多租户）——顺序不能反 |
| 静默失败比不提供更糟 | 每个失败都要让用户知道 + 能重试 |
| 真正上线 ≠ 部署成功 | 真实用户能从看到页面走到拿到权益，链路断一处都不算上线 |

### 我的补充（本项目实践中沉淀）

1. **最短价值闭环**：导入简历 → 一键整份优化 → 全部应用 → 下载 PDF。所有入口都往这条链路上导。
2. **透明化**：范围模糊的指令（「优化简历」）先出计划让用户选；明确指令（「优化整份」）直接执行。过程渐进可见（对比卡逐张出现、完成后列出改了哪几段）。
3. **可撤销 > 二次确认**：能撤销的操作不拦人；不可逆的操作才上应用内确认框。
4. **生产构建实测**（2026-07-04 TDZ 事故教训）：`useCallback` deps 引用后置声明在 dev 能跑、生产压缩后整页崩溃。**改完对话页必须用生产构建（`vite preview`）或线上冒烟一次核心路由。**
5. **可靠验证纪律**：push 后 `git ls-remote` 核对；线上版本用**字面**查找（grep 的 `.` 是正则，`2.8.0` 会误匹配）；部署失败先看服务器 `auto-deploy.log`。

---

## 二、已完成（按版本，全部已上线）

| 版本 | 内容 | 出处建议 |
|---|---|---|
| v2.4.5–2.5.x | 对话粘贴截图解析、glm-ocr 提速、导入超时 150s、联系信息去 emoji 图标、logo 上传鉴权修复 | — |
| v2.5.6 | 管理员会话数无限制 | — |
| v2.6.0 | **首页输入即体验**（hero 输入框替双按钮 + 示例 chip + 兜底链）、fromHome 衔接 | 刘小排文档·第三部分 hero 诊断 |
| v2.6.1 | 博客/GitHub 字段 + \faGithub PDF 真图标 + 三档显示切换 | — |
| v2.7.0 | **一键优化整份简历**（soft 先出计划 / explicit 直接执行、as_completed 渐进出卡、列出优化段）、section 收窄（优化实习只列实习）、技能/自我评价可优化、优化完引导下一段、澄清列表「全部一起优化」+点击即发送、首页粘贴截图、JD 引导卡、导入成功卡、星芒 loading、chip 冷启动引导脚本、技能润色强制无序列表、手机 H1 裁断修复 | 优化功能专项文档·建议1-5 全部 |
| v2.8.0 | **产品打磨批次**：57 处原生 alert/confirm → 应用内 toast + 确认框、停止生成按钮、SSE 出错「重新发送」、保存失败可见（手动+自动）、空态拦截润色/体检、语法体检进度条、润色失败友好提示+重试、PDF 报错可重试、优化应用可撤销、闭环收尾卡（下载 PDF/去编辑器）、会话满一键删最早、JD 会话记忆、优化同义词路由、删语音输入、删 AIPolishDialog 死代码 | 审计 Top 清单大部分 |
| hotfix 057340ee | 修生产构建 TDZ 崩溃（handleStopGeneration 移到 useCLTP 后） | — |
| 未发布（a1d8ebe8 / f869e636 / bafb5005） | ①自动保存常驻指示器（保存中…/已保存/保存失败），主工作台接入 useAutoSaveResume 统一保存模型 ②整份优化对比卡 ≥2 张默认折叠一行摘要、点开看 diff、「全部应用」sticky 置顶 ③优化覆盖面补全：教育经历/荣誉奖项接入单段+整份优化（仅列有描述的条目、不套四要素），并修「点名 section 被上下文劫持」bug | 本文档原 P0-1、P1-2、P1-3；操作记录 [[2026-07-04-保存指示器-折叠对比卡-优化覆盖面]] |

---

## 三、未做的建议（按优先级）

### ✅ P0 · AI 交互 / loading 反馈（已上线 v2.13.0）

> 缘起：v2.12.1 导入解析改思考动画体感变好，顺此思路系统盘了产品里的「等待/反馈」缺口。详见 [[2026-07-06-AI交互loading反馈优化-刘小排视角]]。

- 整份优化进度（loading 样式「已优化 N/M」，不再黑箱、不再套折叠框、不残留历史）
- 导入失败一键重试、回访钩子 chip、开场主动作、应用后预览脉冲、ThoughtProcess 美化
- 修「去编辑器精修」跳到默认简历的 bug
- 撤下「首响思考」（已生效）与「长回答跳打字机」（非真问题）

### ✅ P0 · 主动式 Coco + 交互打磨（已上线 v2.14.0，源自 [[2026-07-06-竞品AI交互调研-简历工具与vibecoding]]）

- 缺口 1+2：单段优化后主动追问「满意吗」+ 一键微调（更简洁 / 更突出成果 / 换更有力的动词），diff 生成时与「应用后」两个时机都有；点击按条目名精确路由回同一段再改一版（走 input-only 匹配、防上下文劫持，维度作为「用户原话」进 LLM prompt）
- PDF 预览工具栏重设计（去红蓝撞色、下载统一 emerald、刷新/关闭图标化）+ 新增「保存」按钮（保存中 / 已保存 / 保存失败状态）

### ✅ P0 · 2026-07-07 连续三批（已上线 v2.14.1 / v2.14.2 / v2.15.0）

- **v2.14.1**：toast「撤销」回灌右侧预览（原 P1 bug 销项）、「开始处理简历」面板 taste 重做、修「从我的简历进 AI 助手硬塞简历」。
- **v2.14.2**：**历史会话恢复不再丢 diff 对比卡/收尾卡/chip**（根因：保存只打包 role/content/thought，patches 只活在内存；修法：ui_state 增存 messageMetas + pendingPatches，按 (role,content,index) 确定性哈希的稳定消息 id 挂回，两处恢复路径统一）——⚠️ localStorage 方案只保本机，跨设备完整回看需后端化（并入「版本时间线」立项）。另：强调色全线 indigo→blue（用户点名去紫）、收尾卡/导入卡白底重做。
- **v2.15.0**：PDF 渲染报错常驻直到成功（独立 renderError state，不再借 progress 推断；原 P1 第 1 条销项）、导出改格式选择弹窗（PDF/JSON，JSON 从仅管理员放开给所有人，「另存为 PDF」入口按用户要求移除）、ui_state 超限防爆（QuotaExceeded 清其它会话后重试）。

### ✅ P0 · 交互修复批次（已上线 v2.14.1，明细见上）

- **修 toast「撤销」不回灌预览的 bug**（原 P1 待办第 2 条，2026-07-07 修复）：根因是 Coco 的 `patchAppliedAt` effect 用「增量套用 applied 补丁」维护预览态，撤销时补丁标回 pending、effect 只加不减。改为直接同步 `ResumeContext.resume`（补丁应用/撤销后的权威当前态）到 `resumeData` + `loadedResumes`——应用照常、撤销天然回灌预览。⚠️ 动了核心补丁应用流，发布前必须实测「应用无回归 + 撤销预览回退」两条。
- **「开始处理简历」面板按 taste-skill 重做**：去 AI 紫渐变 slop（渐变图标 / 洗白渐变底 / blur 光斑全砍），改为实色 indigo 图标 chip + 极浅单色 tint、次选项紧凑横向 icon+label、克制 hover（tint + active 下压，无抬升发光）。
- **修「从我的简历进 AI 助手硬塞简历」bug**：侧边栏「AI 助手」此前无条件带全局 currentResumeId 构造 `/agent/{id}`，任何页面进都弹「从编辑页带来了」。改为只有在编辑页（`/workspace`）时才带简历承接上下文，其它页一律 `/agent/new`（`WorkspaceLayout/index.tsx` 的 `resolveWorkspacePath`）。

### P1 · 核心动作再进一步

1. **缺口 3 选段精修 / 缺口 4 持久撤销 + 版本时间线 + 会话回看后端化**（竞品调研 Top 2、Top 3，中型功能，需立项走 brainstorming + writing-plans）：详见 [[2026-07-06-竞品AI交互调研-简历工具与vibecoding]]。选段精修受限于对话页预览是 PDF blob 不可点选。版本时间线与「会话完整回看跨设备」共用同一底座（快照/meta 存后端），建议一次立项覆盖：每次应用打带描述的简历快照（可回滚可预览旧版）+ 会话 meta/diff 卡存后端（v2.14.2 的 localStorage 方案只保本机）。前置 bug（toast 撤销不回灌预览）已修。
2. **死代码待拍板**：`frontend/src/hooks/agent-chat/useSessionPersistence.ts` 是未接线的抽取副本（约 340 行，CocoChat 实际用自己文件内的实现），2026-07-07 排查历史会话 bug 时确认，容易误导修错文件。建议删除或接线，等用户决定。

### P1 · 产品价值与增长基础（下一步的大杠杆）

2. **低频 → 高频**（建议 0，战略级，**AI 交互小项打磨完后的下一个大方向**）：一份主简历 → 按不同 JD 生成多版本并管理；投递记录；版本历史/对比。这是把「一年用一次」变成「求职季每周用」的关键，也是当前商业模式最大张力的正解。本轮「回访钩子 chip」只是它的最小落点；完整功能需单独走 brainstorming + writing-plans 立项。
3. **Landing 首屏 before/after 视觉**：现在还是产品截图，换成「一句话经历 → 生成简历」对比（细节模糊化，参照 Simplify）。

### P2 · 增长

4. SEO（标题/描述/OG 分享图）、案例页、博客。

### P2 · 工程质量（防回归）

5. **auto-deploy.sh 加 flock 防并发**（2026-07-04 实测两个实例同时构建；2026-07-06 又实测到「部署完成」日志串到上一次、dist 未更新的误判）+ 把脚本纳入仓库管理（现在是服务器上的孤本）。
6. **生产构建冒烟**：CI 或本地脚本跑 `vite build && vite preview`，无头浏览器开 `/`、`/agent/new`、`/workspace` 三条路由查 console error（防 TDZ 类只在压缩产物暴露的崩溃）。
7. code-stats workflow 并发撞锁修复（已改 `concurrency` + push 重试，随本文档一起提交）。

### 暂缓 · 明确不做（不要再出现在建议清单里）

- **商业闭环 / 支付链路**（付费引导时机、额度边界提示、导出/高清/去水印升级引导、价值峰值付费点等）：用户于 2026-07-04 明确决定**暂时不做**，后续打磨建议中不要再列入此类条目，直到用户主动重启该方向。
- **移动端适配**（对话页响应式、预览面板手机布局等）：用户于 2026-07-04 明确决定**暂时不做**，同上，不再列入建议。原问题记录备查：`CocoChat.tsx` 全文无响应式断点；PDF 预览面板 `w-[45%] min-w-[420px]`（`AgentPdfPreviewPanel.tsx:85`）在手机上吃满视口。

---

## 四、下一批推荐顺序

已上线：v2.13.0（AI 交互/loading）、v2.14.0（主动式 Coco + 预览工具栏/保存）、v2.14.1（撤销回灌/面板重做/nav 修复）、v2.14.2（历史会话恢复对比卡 + 全线换蓝）、v2.15.0（导出弹窗 + PDF 报错常驻 + ui_state 防爆）。
**下一批：⭐版本时间线 + 持久撤销 + 会话回看后端化（中，一次立项三收益，走 brainstorming + writing-plans）> 选段精修（中，立项）> Landing before/after（中）> 低频→高频（大，单独立项）。**
穿插小项：死代码 useSessionPersistence 拍板、auto-deploy flock、生产构建冒烟（见 P2 工程质量）。
商业闭环/支付链路与移动端适配已明确暂缓（见「暂缓」节），不参与排序。
