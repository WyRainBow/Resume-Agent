# AI 交互 / loading 反馈优化（刘小排视角）

> 日期：2026-07-06
> 缘起：v2.12.1 把「导入解析」从静态文案改成思考动画（星芒旋转+文案脉动），体感明显变好。
> 本文用刘小排的产品思维，系统盘一遍产品里还有哪些「等待/反馈」环节能用同样思路打磨。
> 关联：[[2026-07-03-刘小排产品思维与Resume-Agent产品设计]]（方法论）· [[2026-07-06-Agent对话交互优化分析]]（技术向全链路审查）· [[2026-07-04-产品打磨路线图]]（唯一待办）

---

## 一、方法论：刘小排里跟「交互反馈」直接相关的三把尺子

| 原则 | 一句话 | 落到交互上 |
|---|---|---|
| **买时间** | 每一步只争取下一步；别让用户在无反馈里干等 | 任何 >1s 的等待都要有「活的」反馈，且尽量给出**边界感**（还要多久/还剩几步） |
| **结果直接可见** | 做完立刻看到效果 | 结果一产出就呈现（右侧预览、对比卡、before-after），不让用户再点一下才看到 |
| **静默失败比不提供更糟** | 每个失败都要让用户知道 + 能重试 | 不 silent catch；失败要有明确文案 + 一键重试，不让用户重新组织语言 |

**判据**：产品里每一个「用户发起动作 → 系统在忙 → 结果出来」的环节，问三句——
① 忙的时候用户看到的是「活的反馈」还是「死等/灰按钮」？
② 结果出来是「自动可见」还是「要用户再操作」？
③ 万一失败，用户「知道且能一键重试」还是「一脸懵」？

---

## 二、已经做对的（作为参照样板）

- **导入解析 → 思考动画**（v2.12.1，本次）：图片/文本/文件三入口的解析等待都用 `ThinkingIndicator`，并隐藏了占位消息上不该有的复制/赞/踩。✅ 买时间
- **整份优化 → 对比卡渐进出现**（v2.7.0）：`as_completed` 让每段优化完就出一张卡。✅ 过程渐进可见
- **保存状态常驻指示器**（v2.9.0）：保存中…/已保存/保存失败。✅ 结果可见 + 失败不静默
- **PDF 预览不闪烁 + 保留旧版**（v2.12.0）：重渲染时旧 PDF 持续可见到新的就绪。✅ 结果直接可见
- **SSE 出错「重新发送」、PDF 报错可重试、润色失败重试**（v2.8.0）：✅ 静默失败比不提供更糟
- **流式思考占位**（`StreamingOutputPanel` 用 `ThinkingIndicator`）：思考首字未到时星芒占位。✅ 买时间

**结论**：基础反馈能力已经不错。下面是仍有「死等/无边界/结果需二次操作/静默失败」的具体缺口。

---

## 三、可优化点（按价值排序，均为交互反馈类，不含已暂缓的支付/移动端）

### P0-A · 整份优化缺「进度边界感」——最该做，且最贴本次思路

**现状**：用户说「优化整份简历」后，后端 `_optimize_whole_resume` 直接跑 ~30s，中途靠对比卡逐张冒出来，但**没有「共 N 段、已完成 M」的进度**，也没有开场的「正在逐段优化（共 8 段）…」预期。用户面对的是「一张张卡慢慢冒，但不知道还有几张、还要等多久」。

**踩了哪条尺子**：买时间——长等待**没有边界感**是焦虑主因；刘小排强调每一步都要让用户知道「进行到哪了、还要多久」。

**建议**：
- 开场立即出一条**带进度的思考消息**：「正在逐段优化你的简历（共 N 段），完成一段出一张对比卡…」——N 用 `list_optimize_targets` + 技能/自我评价数，**开跑前就能算出**。
- 每入队一张卡就更新进度：「已完成 3/8…」。可复用 `ThinkingIndicator` + 一个 `x/N` 计数，或做个细进度条。
- 技术落点：后端在 `_optimize_whole_resume` 开头先发一个 `optimize_plan`/进度 SSE 事件（总数 N），每段 `as_completed` 后发 `optimize_progress`（done/total）；前端把这条思考消息随事件更新。
- **价值**：把「30s 黑箱等待」变成「8 段进度条」，是本次「导入思考动画」思路的自然延伸，且优化是核心动作，收益最大。

### P0-B · 发消息 → 首个 SSE 之间的「死等窗口」

**现状**（见 [[2026-07-06-Agent对话交互优化分析]] P1-4）：用户发消息后，经历「网络 RTT + 后端建会话 + 首次 LLM 调用（2-5s）」才有第一个事件。这段时间只有输入框变灰（`isProcessing`），**没有思考动画**。首次响应延迟感强。

**踩了哪条尺子**：买时间——最该有反馈的恰恰是「刚点完、还没任何回音」的头几秒。

**建议**：`sendMessage` 一旦 `isProcessing=true`，立即在对话流末尾插入 `ThinkingIndicator`（「思考中…」），首个 `thought/answer_chunk` 到达再替换。项目已有组件，改动小、体感立竿见影。

### P1-C · 全站失败态统一「可重试」审计

**现状**：部分失败已可重试（SSE 重新发送、PDF 报错、润色失败），但仍有零散的 `catch { toast.error }` 后**要用户重新发起**的点（如图片识别失败、导入解析失败、评分失败）。

**踩了哪条尺子**：静默失败比不提供更糟——失败不仅要提示，还要**能一键重试**，不让用户重新组织输入。

**建议**：盘一遍所有 `upload-image/upload-pdf/parse/score` 失败分支，失败消息统一带「重试」按钮（重发同一份文件/同一段文本），而不是只弹 toast。断线场景加输入框上方 banner（分析文档 P3-13）。

### P1-D · 长回答跳过打字机，别让「结果可见」被动画拖慢

**现状**（分析文档 P3-15）：Agent 长回答（几百字）走打字机效果，用户要等很久才看到完整内容。

**踩了哪条尺子**：结果直接可见——动画本意是「活」，但过长的打字机反而**延迟了结果可见**，与原则相悖。

**建议**：长回答（>200 字）跳过打字机直接整段显示；短回答保留打字机的「活」感。

### P2-E · PDF 渲染进度阶段化（锦上添花）

**现状**：闪烁已修（保留旧 PDF）。但渲染进度文案仍是笼统的「正在渲染 PDF...」。

**建议**（可选）：把进度分阶段（准备数据 → 排版编译 → 生成预览），让 3-8s 的 LaTeX 编译更有过程感。优先级低，因为旧 PDF 已保留、体感的「空白焦虑」已解决。

---

## 四、进度（2026-07-06 更新）

- ✅ **P0-A 整份优化进度**：已实现（分支 `docs/ai-interaction-polish`，commit `c8b40e21`）。开跑报总段数，每段完成实时推「✓ 已优化「X」（n/N）」，SSE 序列已验证。
- ✅ **P0-B 首响思考动画**：盘查后确认**已生效**（发消息即显示「思考中…」，`StreamingOutputPanel` 已处理），无需改。
- ✅ **P1-C 失败可重试**：图片/文件导入失败已加「重试」按钮（重发同份文件），commit `c8b40e21`。评分/断线 banner 仍可后补。
- ⏭️ **P1-D 长回答跳打字机**：盘查后确认**不是真问题**——主对话 `StreamingResponse` 直接渲染流式内容、无慢速打字机（analysis doc P3-15 针对旧实现/AIWriteDialog）。撤下。
- ⏭️ **P2-E PDF 渲染阶段化**：可选，优先级低。

---

## 五、更多优化方向（2026-07-06 新增，按刘小排思想，未做）

> 上面 P0/P1 是「等待/失败反馈」维度。这一节从**低频→高频、核心动作明确、结果直接可见**三条尺子挖更深的交互/产品方向，仍不碰暂缓的支付/移动端。

### ⭐ 方向 1 · 优化收尾卡加「回访钩子」——低频→高频的最小交互落点（刘小排建议 0，战略级）

**现状**：优化应用完成的收尾卡（`ImportSuccessCard.tsx` 的 `ApplyDoneCard`）只给两个下一步：**下载 PDF / 去编辑器精修**。都是「这次用完就走」的终点。

**踩了哪条尺子**：刘小排反复强调**简历低频是最大张力，必须主动把低频做成高频**；而「刚优化完」正是价值峰值——最该埋下一次使用的种子的时刻，现在却让用户走了。

**建议**：收尾卡加一个 chip「🎯 针对某个岗位再优化一版」/「换个 JD 再来一次」。点击 → 进 JD 引导卡 → 生成针对该岗位的一版。这是把「一次性优化」变成「投 N 个岗用 N 次」的**最低成本落点**（无需先做完整的多版本管理，先用一个 chip 把回访动作显性化）。收益：直接对齐刘小排最强战略杠杆，改动小（复用已有 JD 引导卡 + 建议 chip 机制）。

### 方向 2 · AI 主动给「一个主动作」，而非让用户做选择题（核心动作明确 + 买时间）

**现状**：加载简历后 Coco 开场是「已加载「X」，右侧可以看到。**想优化哪部分**？直接告诉我，比如「优化实习经历」。」——把选择权丢回用户（想优化哪部分 = 一道选择题）。

**踩了哪条尺子**：刘小排——**一屏一个主 CTA、用户一句话能理解、每步只争取下一步**。开放式提问增加决策成本。

**建议**：开场主动提议**单一最高价值动作**做主 CTA，例如「要不要我先帮你**一键优化整份**？」（一个醒目按钮）+ 次要的「或者先做份体检 / 按 JD 改」。把「你想干嘛」变成「我建议这么干，一键即可」。复用现有 `%%SUGGESTIONS%%` 建议 chip，改的是开场文案与主次层级。

### 方向 3 · 应用优化后「结果直接可见」——预览高亮/滚动到刚改的段（结果直接可见）

**现状**：`applyPatches` 后 bump `patchAppliedAt` → 触发右侧 PDF 重渲染，但**不高亮、不滚动**到改动段。用户知道「PDF 变了」，但要自己找「改了哪」。

**踩了哪条尺子**：结果直接可见——做完要让用户**立刻看到效果在哪**。

**建议**：应用后把预览滚动到 / 高亮刚改的 section（对话里已有 before/after diff 卡，这条是**预览侧**的补强）。优先级中（diff 卡已部分满足「可见」）。

### 方向 4 · 断线恢复提示（静默失败比不提供更糟，接 P1-C）

**现状**：SSE 断线时 `useCLTP` 设 `lastError`，聊天框里显示错误文字，但用户不知道能否继续。

**建议**：断线后输入框上方显示常驻 banner「连接已断开，恢复后可继续」+ 重连。属 P1-C「失败不静默」的收口，一并做。

---

## 六、实施进度（2026-07-06，分支 `docs/ai-interaction-polish`，未合并未推送）

第五节四方向 + 若干补强已全部落地：

| 项 | 状态 | commit |
|---|---|---|
| 方向 1 回访钩子（低频→高频） | ✅ 收尾卡加「针对某个岗位再优化一版」chip → 打开 JD 卡 | `c5044e1b` |
| 方向 2 AI 主动主动作 | ✅ 加载简历后开场改为主动推荐「一键优化整份」+ 通用 `meta.suggestions` chip（首个为主 CTA） | `c5044e1b` |
| 方向 3 结果直接可见 | ✅ 应用优化后预览面板脉冲高亮（1.6s ring）引导视线（PDF 无法逐段锚定，改用整体脉冲） | `c5044e1b` |
| 方向 4 断线不静默 | ✅ 确认已有（`lastError`→`resumeError` 红横幅 + 重新发送），未加重复代码 | 既有 |
| 整份优化进度改 loading 样式 | ✅ 进行中从 ThoughtProcess 折叠框改为星芒旋转+脉动清单 | `1a9aa395` |
| 优化进度不残留成历史「思考过程」 | ✅ 完成后不再以折叠框留在历史 | `e7ae6c93` |
| ThoughtProcess 美化 | ✅ 英文→中文「思考过程」+ 脑图标 + 胶囊头 + 左边线内容区 | `e7ae6c93` |
| **修 bug**：去编辑器跳到默认简历 | ✅ 收尾卡「去编辑器」原跳无 ID 的 `/workspace/latex`（被当新建默认模板）→ 改为先回存优化数据再带 ID 跳 | `1a9aa395` |

**盘查后撤下的**：首响思考动画（已生效）、长回答跳打字机（非真问题，主对话直接渲染流式）。

---

## 七、AI 交互层已基本打磨完，下一步的大杠杆

到此，「等待/反馈 + 核心动作 + 结果可见 + 失败不静默」这几条尺子在 AI 对话链路上的**小步交互缺口已基本补齐**。再往下不是零碎交互，而是刘小排的**战略级建议 0：低频 → 高频**（本轮方向 1 的「回访钩子」只是它的最小落点）：

- **一份主简历 → 按不同 JD 生成并管理多版本**（投 N 个岗 = 用 N 次）；
- **投递记录 / 版本历史 / 对比**，给用户回访理由；
- 「这个岗位建议用哪一版」的智能推荐。

这是把「一年用一次」变成「求职季每周用」的关键，也是当前商业模式最大张力的正解。**但它是一个大功能，需单独走 `/superpowers:brainstorming` + `writing-plans` 立项**，不宜作为零散交互项塞进本轮。已在 [[2026-07-04-产品打磨路线图]] 记为 P1「低频→高频」。

其余可选小项（低优先）：PDF 渲染进度阶段化（P2-E）、断线 banner 挪到输入框上方（现在在消息流里，功能已具备）。**均不涉及支付闭环 / 移动端**（已暂缓）。
